# kite-ai-flow

Kite AI CMO — GitHub integration architecture (v0.5).

One flow, two surfaces (`appsmith.kite.space` + customer's own deploy), two-way sync scoped to `kite/*` branches. Combines the previous gold/blue lines instead of forking them.

Open `index.html` in a browser.
